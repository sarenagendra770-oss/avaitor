document.addEventListener('DOMContentLoaded', () => {
  const nameField = document.querySelector('.profile-card p');
  if (nameField) {
    nameField.textContent = 'Manage your identity, settings, and activity history.';
  }
});
